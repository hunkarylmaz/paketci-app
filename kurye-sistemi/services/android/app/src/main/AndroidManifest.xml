<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.paketci.service"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PERMISSIONS -->
    <!-- Location -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    
    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /

    <!-- Phone / Caller ID -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" /
    <uses-permission android:name="android.permission.CALL_PHONE" /
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" /

    <!-- Bluetooth Printer -->
    <uses-permission android:name="android.permission.BLUETOOTH" /
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" /
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" /
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" /

    <!-- USB Printer -->
    <uses-permission android:name="android.permission.USB_PERMISSION" /
    
    <!-- Boot & Wake Lock -->
    <uses-permission android:name="android.permission.WAKE_LOCK" /
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" /

    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" /

    <!-- Features -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".PaketciApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Paketci"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- MAIN ACTIVITY -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- CALLER ID POPUP ACTIVITY -->
        <activity
            android:name=".CallerIDPopupActivity"
            android:theme="@style/Theme.CallerIDPopup"
            android:launchMode="singleTop"
            android:excludeFromRecents="true"
            android:showOnLockScreen="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:exported="false" />

        <!-- FOREGROUND SERVICES -->
        
        <!-- Konum Takip Servisi -->
        <service
            android:name=".service.PaketciService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location|dataSync" />

        <!-- FCM Push Notification Servisi -->
        <service
            android:name=".service.PaketciFirebaseMessagingService"
            android:exported="false"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Bluetooth Yazıcı Servisi -->
        <service
            android:name=".service.BluetoothPrinterService"
            android:enabled="true"
            android:exported="false" />

        <!-- RECEIVERS -->
        
        <!-- Call Receiver (Caller ID) -->
        <receiver
            android:name=".receiver.CallReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_STICKY"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver (Cihaz açılışında servisi başlat) -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- FCM Configuration -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/paketci_blue" /
        
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="true" />

    </application>

</manifest>
